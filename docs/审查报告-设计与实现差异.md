# 设计文档 vs 实际实现 —— 差异审查报告

> 审查日期：2026-08-01
> 审查范围：`docs/项目规划.md` · `docs/ui-notes.md` · `docs/task-system-spec.md` vs 当前代码
> 审查方式：逐项核对文档声明的功能与代码实现，定位差异并给优化建议

---

## 一、文档过时项（实际已完成，文档未更新）

> 这些项**不影响运行**，但文档与代码不一致会让新人误读。建议尽快同步。

| # | 差异点 | 文档说 | 实际 | 影响 |
|---|--------|--------|------|------|
| 1 | 模板数量 | 15 个 | **24 个** | 中 — 新增 desk_list/extract/outline_generator/settings/timeline_editor/timeline_outline_card/timeline_write_flow/writing_desk/new_book_flow |
| 2 | E2E 测试规模 | 12 页 + 11 链接 | **16 页 + 15 链接** | 低 |
| 3 | `engine.py` 行数 | 690 行 | **1270 行** | 低 — 规模信息失真 |
| 4 | `beat_writer.py` 行数 | 550 行 | **759 行** | 低 |
| 5 | 笔名档案文件名 | `profile.py`（单数） | **`profiles.py`（复数）** | 高 — 若按文档 import 会失败（纯笔误，代码无引用） |
| 6 | `writing_pipeline.py` | 标注 `[已废弃]` | **已删除** | 低 — 应移除目录树条目 |
| 7 | `streamlit_app.py` | 标注 `[已废弃]` | **已删除** | 低 — 应移除目录树条目 |
| 8 | `write.html` | "写作面板" | **已删除**，`/write` redirect 到 `/books`（不 404） | 低 |
| 9 | 任务缓存文件 | `plugins/task_manager/tasks.json` | 根目录 **`_api_tasks.json`** | 高 — 路径与文件名全错（task_manager 实际不直接读写它） |
| 10 | 顶层目录 | 未提及 | 新增 `frontend/` `static/` `storage/` `tools/` `api/`(空) | 中 — 结构变更无文档 |
| 11 | timeline 设计稿 | `docs/novel-factory-timeline.html` | `docs/` 与 `static/` **各一份、内容不同**（static 为更新版） | 低 |

### 未列入目录树但已存在的文件

- `libraries/`：`outline_generator.py`（大纲引擎）、`timeline_writer.py`、`timeline.py`、`reset_data.py`、`data/`（四库 JSON）
- `plugins/`：`novel_storage.py`、`style_analyzer.py`
- `tools/`：`e2e_flow_test.py` + `shots/`（浏览器自动化测试）

---

## 二、文档诚实但确实未做（P0-2 ~ P0-7）

> 文档都标了 `[ ]` 待办，代码里也真的没有。这是 show-me-the-story 的工程实践，**全部未移植**。

| 项 | 现状 | 预期价值 |
|----|------|---------|
| **P0-2 Volume 压缩** | `core/inject.py:36-40` 逐章列出过去大纲（100 章书约 90 行） | 压缩为 `【《卷名》卷摘要】` 1 行，**token 节约 85%+** |
| **P0-3 大纲长度验证+重试** | `outline_generator.py:_validate` 仅规则检查，无自动重试 | 消除 AI 偷懒写 50 字大纲的问题 |
| **P0-4 段落级原文引用** | `character_state.py`/`inject.py` 只存摘要 | 注入 prompt 时连带原文 snippet，AI 写得准 |
| **P0-5 自适应 context budget** | `api.json` 有 `context_budget_tokens` 但只存不计算 | 长书不会 context overflow |
| **P0-6 段落边界尾提取** | `core/inject.py:27` 字符硬截断 | 按 `\n\n` 取完整段落 |
| **P0-7 Config Snapshot** | 全项目零代码（仅文档提到） | 大纲生成时存配置快照 `config_snapshot.json` |

---

## 三、半做项（有逻辑/API，但未接入主 UI 或引擎）

| 项 | 现状 | 缺口 |
|----|------|------|
| **全书优化诊断** | `core/reconcile.py:book_diagnosis` + `main.py:836 /api/book/diagnosis` | **`web_ui.py`（主面板）无此路由**，用户点不到 |
| **伏笔系统** | `core/foreshadow.py` 完整 + `main.py` 有全套 API | **`engine.py` 用自带简化版 `_generate_foreshadows`，未用 `foreshadow.py`**；web_ui 无伏笔 UI |
| **设定协调** | `main.py:817 /api/settings/reconcile` | **web_ui 未接入** |
| **大纲引擎 Phase 4/5** | `outline_generator.py:534/573` 是纯规则 | 文档设计为「规则 + 1 次 LLM 复查」（Phase 4）、「1 次 LLM 验证」（Phase 5），**LLM 调用缺失** |

---

## 四、已确认正常 ✅

- Phase 1 全部 11 项（DeepSeek 流式封装 / 四大库+UI / 单书管理 / AI 降重 / 内容审阅 / 番茄侦察兵 / BookAssembler / 任务管理 / E2E 测试 / 费用追踪）**真实实现**
- P0-1（engine ↔ timeline_writer 断链修复，文档标 ✅ 7/31）**确已修复**：`engine.py:308-310` 创建 `TimelineChapterWriter`，`:1076` 调用
- `ui-notes.md` 声明的 4 项 UI 改进**均已实现**

---

## 五、优化建议（按优先级）

### 高价值：提升写作质量 / 降低成本

1. **P0-2 Volume 压缩**（`core/inject.py`）— 改动小、收益大（长书上下文节约 85%），与 P0-6 同文件可一并做
2. **P0-6 段落边界尾提取**（`core/inject.py:27`）— 按 `\n\n` 取完整段落
3. **大纲引擎补 LLM 复查**（`outline_generator.py` Phase 4/5）— 让生成大纲有 AI 质量把关

### 中价值：补上主 UI 缺口

4. **web_ui 接入全书诊断 / 伏笔 / 设定协调** — 逻辑和 main.py API 都在，只是 Flask 面板没暴露入口

### 低价值：纯文档维护

5. **更新 `项目规划.md`** — 修正第一节全部过时项，补全新文件

---

## 六、附：验证过的关键事实

- `write.html` 在 `web_ui.py` 无路由引用，`/write` 是 redirect 非 404
- `profile.py`（单数）在全项目**零代码引用**，确为文档笔误
- `_api_tasks.json` 为运行时缓存，`task_manager.py` 不直接读写它（任务在内存 + 前端轮询）
- 大纲生成器 `_inject_gags_and_themes` / `_validate` 确认**无 LLM 调用**，与设计规格不符
